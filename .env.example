# ──────────────────────────────────────────
# Break the Loop — Environment Configuration
# ──────────────────────────────────────────
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# ──────────────────────────────────────────

# Prisma — SQLite database path (relative to prisma/ directory)
DATABASE_URL="file:./dev.db"

# Piston — Code execution engine API endpoint
# Default: http://127.0.0.1:2000/api/v2/execute
# Use 127.0.0.1 instead of localhost to avoid IPv4/IPv6 mismatch on Windows
PISTON_API_URL="http://127.0.0.1:2000/api/v2/execute"
